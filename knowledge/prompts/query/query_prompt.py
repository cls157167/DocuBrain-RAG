"""查询流程相关提示词模板

定义LLM 调用所需的提示词模板。
"""

# 商品名称提取提示词模板
ITEM_NAME_EXTRACT_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的商品名称（item_names）。
1. 如果当前问题与商品/产品/设备完全无关（如问天气、时间、闲聊等），item_names 返回空列表，rewritten_query 保持原问题不变。
2. 如果用户明确提到了商品名称，请提取出来。可能有一个或多个，但不能重复。
3. 如果用户使用了代词（如"这个"、"它"），且当前问题明显是在询问商品相关事项，请结合历史会话指代消解，确定商品名称。
4. 如果用户表达了确认或肯定（如"是的"、"对"、"嗯"、"好"、"确认"、"可以"等），请从历史会话中提取助手最近一次提到的商品名称作为 item_names，并将当前问题改写为对该商品的完整查询。
5. 如果无法确定商品名称，item_names 返回空列表。
6. 请重新改写用户的问题（rewritten_query），使其成为包含商品名称的独立完整问题。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["商品A", "商品B"],
    "rewritten_query": "关于商品A和商品B，..."
}}"""



# 假设性文档提示词
USER_HYDE_PROMPT_TEMPLATE = """请模拟一段产品技术文档/说明书中的内容，该内容能够回答以下用户问题。

相关产品名:
{item_hint}

用户问题：
{rewritten_query}

要求：
1. 以产品说明书、操作手册或技术文档的口吻撰写，使用陈述句
2. 直接描述事实和操作步骤，不要出现"假设"、"可能"、"大概"等不确定用语
3. 包含具体的技术细节、部件名称、操作步骤等关键词,保持回答与查询主题高度相关
4. 不需要标题和格式标记，只输出正文段落
5. 使用中文，300-350字
"""


# HyDE假设性文档生成提示词
HYDE_SYSTEM_TEMPLATE = """你是一名资深的技术文档撰写专家。
你的任务是：根据给定的商品名称和用户问题，凭空撰写一篇"假设性产品技术文档"。
这篇文档不是真实存在的，但它应该包含能够回答用户问题的关键信息和术语，读起来就像从知识库中检索出来的真实技术资料。

核心原则：
- 文档内容必须专业、详实，使用说明文/技术文档的陈述语气
- 围绕给定的商品名称展开，紧密贴合用户问题的核心意图
- 尽可能包含具体的型号、参数、部件名、操作动词等技术关键词
- 不要出现"假设"、"虚拟"、"模拟"、"可能"、"大概"等暴露文档虚构性的词汇
- 不需要标题、编号和格式标记，只输出一段连贯的正文"""


HYDE_HUMAN_TEMPLATE = """请根据以下信息，撰写一篇假设性产品技术文档段落。

【涉及商品】
{item_names}

【用户想要了解的问题】
{rewritten_query}

【撰写要求】
1. 以产品说明书或技术手册的口吻，用陈述句直接描述
2. 围绕上述商品展开，内容要与用户问题高度相关，覆盖用户可能关心的知识点
3. 嵌入具体的技术细节和关键词（如部件名称、规格参数、操作流程、注意事项等），提高文档的信息密度
4. 篇幅控制在200-350字，只输出正文段落，不要添加标题、序号或markdown标记
5. 使用中文撰写，语言专业流畅"""


# HyDE System Message
HYDE_SYSTEM_MESSAGE = """你是一名资深的技术文档撰写专家，擅长为各类工业设备和机械产品撰写产品说明书、技术手册、操作指南和故障排查文档。你生成的文档内容专业、准确、详实，读起来就像真实的官方技术资料。"""


# 【图谱中存在的实体类型】
# {allowed_entity_labels_cn}

# LLM实体名提取提示词模版
ENTITY_EXTRACT_SYSTEM_PROMPT = """
你是一个知识图谱问答系统的"实体识别"领域专家。
请从用户问题中抽取用于查询图数据库(Neo4j)的实体名称。


【约束】
1) 优先抽取上述类型的名词短语（设备名、部件名、操作名、工具名、步骤名、条件、警告等）
2) 每个实体名称不超过 {MAX_ENTITY_NAME_LENGTH} 个字符，超过请截取核心部分，实体名不要重复
3) 不要输出完整句子，也不要错误的把商品名识别为实体名字，只输出实体关键词
4) 输出必须是严格 JSON，只含一个字段 entities（字符串数组）
5） 请严格遵循提示词的要求，如果违背，你将受到严重的惩罚，甚至导致系统的崩溃。

【输出示例】
{{"entities": ["实体名字1", "实体名字2", "实体名字3"]}}
"""


# 回答生成提示词模板
ANSWER_PROMPT = """你是一个智能助手，请根据参考内容回答用户的问题。
要求：
1 尽量基于【参考内容】作答，不要编造不存在的事实。
2 如果用户的问题需要通过图片来辅助说明（例如：外观、结构、接线、示意图等）,图片只能来自于本地切片文本中的图片，请在答案最后追加一个独立的图片区块，格式严格如下：
【图片】
<图片URL1>
<图片URL2>
（每行一个URL；如果没有合适图片则不要输出【图片】区块）

【参考内容】
{context}

【历史对话】
{history}

【相关商品/实体】
{item_names}

【用户问题】
{question}

请回答："""